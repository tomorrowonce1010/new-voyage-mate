# 数据库配置
# 重要：必须通过环境变量设置这些值！
# 本地开发请在启动前执行：source backend/set-env-local.sh
# 生产环境请在系统中设置环境变量
spring.datasource.url=jdbc:mysql://localhost:3306/voyagemate?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useAffectedRows=true
spring.datasource.username=voyagemate
spring.datasource.password=se_202507
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 服务器配置
server.port=8080
server.servlet.context-path=/api
en

# 日志配置
logging.level.com.se_07.backend=DEBUG
logging.level.org.springframework.web=DEBUG

# RAG服务配置
rag.service.url=http://localhost:8001
rag.service.timeout=30000

# 跨域配置
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# 缓存配置
spring.cache.type=simple
spring.cache.cache-names=attractions,cities,users

# 安全配置
spring.security.user.name=admin
spring.security.user.password=admin123


# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.current_session_context_class=org.springframework.orm.hibernate5.SpringSessionContext
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jpa.open-in-view=true
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Shanghai
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.jdbc.batch_size=25
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true
spring.jpa.properties.hibernate.connection.characterEncoding=utf8
spring.jpa.properties.hibernate.connection.useUnicode=true
spring.jpa.properties.hibernate.connection.CharSet=utf8
spring.jpa.properties.hibernate.hbm2ddl.auto=update
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.generate_statistics=false
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=false




# 静态资源配置
spring.web.resources.static-locations=classpath:/static/
spring.mvc.static-path-pattern=/static/**

# 日志配置
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# 日志格式配置
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# 文件上传配置
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# 时区配置
spring.jackson.time-zone=Asia/Shanghai

# Session配置（使用内存存储）
server.servlet.session.timeout=24h
server.servlet.session.cookie.name=JSESSIONID
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false

# DeepSeek AI配置
# 重要：必须通过环境变量设置 DEEPSEEK_API_KEY！
# 本地开发请在启动前执行：source backend/set-env-local.sh
deepseek.api.key=${DEEPSEEK_API_KEY}
deepseek.api.url=${DEEPSEEK_API_URL:https://api.deepseek.com/v1/chat/completions}

# Elasticsearch
spring.elasticsearch.host=localhost
spring.elasticsearch.port=9200

# Embedding service (Python)
embedding.service.url=http://localhost:8000/embed

# Semantic index name
semantic.index.name=destinations

# Attraction semantic search configuration
semantic.attraction.index.prefix=attractions_destination_

logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=INFO

# Actuator配置 - 健康检查端点
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.health.defaults.enabled=true